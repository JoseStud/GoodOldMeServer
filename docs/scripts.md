# Scripts & Utilities

This document catalogs operator-facing helper scripts in `scripts/` plus the direct CI entrypoints in `.github/scripts/` that workflows invoke. Shared library helpers under `.github/scripts/lib/` are intentionally omitted from the main catalog.

## Catalog

| Script | Purpose | Parameters (args/env) | Exit code behavior | Idempotency | Required tools/dependencies | Example |
|-------|---------|------------------------|--------------------|-------------|-----------------------------|---------|
| `scripts/cloudflare-dns.sh` | Create or update a Cloudflare DNS A record for `<stack>.<BASE_DOMAIN>`. | Args: `<STACK_NAME> <IP_ADDRESS>`. Env: `ZONE_ID`, `CLOUDFLARE_API_TOKEN`, `BASE_DOMAIN`. | `0` on successful create/update, `1` on usage/validation/API errors. | Conditionally idempotent (updates existing record, creates if missing). | `bash`, `curl`, `jq`, Cloudflare API access. | `./scripts/cloudflare-dns.sh portainer 192.168.1.50` |
| `scripts/portainer-webhook.sh` | Trigger one or more Portainer GitOps webhooks. | Args: one or more webhook URLs, or env `WEBHOOK_URLS` (CSV). | `0` when all calls return `200/204`; `1` if usage invalid or any webhook fails. | Not idempotent at workflow level (re-trigger requests), but safe for redeploy semantics. | `bash`, `curl`. | `WEBHOOK_URLS="https://.../uuid1,https://.../uuid2" ./scripts/portainer-webhook.sh` |
| `scripts/import_infisical_secrets.sh` | Seed operator-managed Infisical secrets with placeholders while intentionally skipping automation-managed outputs (Portainer bootstrap credentials, allowlist sync output, webhook URLs). The seeded values are intentionally fail-closed and must be replaced before preflight succeeds. | Env: logged-in Infisical CLI context, `INFISICAL_PROJECT_ID`; fixed `ENV=prod` in script. | Uses `set -euo pipefail`; exits non-zero on missing `INFISICAL_PROJECT_ID` or unrecoverable shell errors, while each `infisical secrets set ... || true` still suppresses per-key write failures. | Mostly idempotent for initial seed (re-runs overwrite existing values). | `bash`, `infisical` CLI auth/session. | `INFISICAL_PROJECT_ID=... ./scripts/import_infisical_secrets.sh` |
| `scripts/archive/portainer-deploy.sh` *(archived)* | Legacy REST update path for Portainer stack env vars. | Args: `<stack_name> <env_file_path>`. Env: `BASE_DOMAIN`, `PORTAINER_TOKEN`, optional `ENDPOINT_ID`. | `0` on HTTP `200/204`; `1` on validation/not-found/API failures. | Conditionally idempotent for same env payload. | `bash`, `curl`, `jq`, `grep`, `sed`. | `BASE_DOMAIN=example.com PORTAINER_TOKEN=... scripts/archive/portainer-deploy.sh gateway .env` |
| `scripts/archive/update_deploy.py` *(archived)* | One-time migration helper to add `update_config.order: start-first` across compose files. | No args. Hardcoded file list. | Always exits `0` unless Python/file I/O fails. | Idempotent after target files already contain desired config. | `python3`. | `python3 scripts/archive/update_deploy.py` |
| `.github/scripts/tfc/render_inventory_from_tfc_outputs.sh` | Build deterministic `inventory-ci.yml` from TFC outputs. | Args: `<workspace_name> [output_file]`. Env: `TFC_TOKEN`, `TFC_ORGANIZATION`, optional `TFC_API_URL`. | `0` on success; non-zero on missing env/outputs/API failures. | Idempotent for same workspace state/output path. | `bash`, `curl`, `jq`. | `TFC_TOKEN=... TFC_ORGANIZATION=my-org .github/scripts/tfc/render_inventory_from_tfc_outputs.sh goodoldme-infra` |
| `.github/scripts/stacks/trigger_webhooks_with_gates.sh` | Dependency-ordered full-reconcile webhook trigger with pre/post health gates from `stacks/stacks.yaml` for all Portainer-managed stacks. | Args: `<manifest_path>`; env `WEBHOOK_URL_*`, optional `BASE_DOMAIN`. | `0` on successful ordered redeploy; `1` on missing webhook, health timeout, dependency cycle, etc. | Partially idempotent (re-triggers redeploys; health checks are repeat-safe). | `bash`, `curl`, `yq`, manifest file. | `.github/scripts/stacks/trigger_webhooks_with_gates.sh stacks/stacks.yaml` |
| `.github/scripts/network/build_network_access_policy.sh` | Build canonical `network_access_policy` JSON from runner public IPs or break-glass override. | Env: optional `BREAK_GLASS_POLICY_JSON` / `BREAK_GLASS_POLICY_FILE`; optional `GITHUB_OUTPUT`. | `0` on valid policy; `1` on schema/family validation failures or dependency errors. | Idempotent for same override or same observed runner IPs. | `bash`, `curl`, `jq`, `python3`. | `BREAK_GLASS_POLICY_JSON='{"oci_ssh":{"enabled":true,"source_ranges":["203.0.113.10/32"]},"gcp_ssh":{"enabled":true,"source_ranges":["2001:db8::/128"]},"portainer_api":{"source_ranges":["203.0.113.10/32","2001:db8::/128"]}}' .github/scripts/network/build_network_access_policy.sh` |
| `.github/scripts/network/sync_network_access_policy.sh` | Atomically sync policy to Terraform Cloud var and the explicitly project-scoped Infisical allowlist secret, then verify read-after-write. | Env: `NETWORK_ACCESS_POLICY_JSON`, `TFC_TOKEN`, `TFC_ORGANIZATION`, `TFC_WORKSPACE_INFRA`, `INFISICAL_PROJECT_ID`. | `0` on full sync+verify success; `1` on API/set/get mismatches or missing env. | Idempotent for same policy JSON. | `bash`, `curl`, `jq`, `infisical` CLI auth/session. | `NETWORK_ACCESS_POLICY_JSON='...' INFISICAL_PROJECT_ID=... .github/scripts/network/sync_network_access_policy.sh` |
| `.github/scripts/network/preflight_network_access.sh` | Fail-closed preflight for runner egress policy and optional SSH/Portainer reachability checks. Only validates the access families required by the current run and honors disabled SSH families unless that run actually needs them. | Env: `NETWORK_ACCESS_POLICY_JSON`; optional toggles `RUN_ANSIBLE`, `RUN_HOST_SYNC`, `RUN_CONFIG`, `RUN_HEALTH`, `RUN_PORTAINER`, `PORTAINER_API_URL`, `INVENTORY_FILE`. | `0` when all required checks pass; `1` when policy mismatch or reachability checks fail. | Idempotent for same policy and network state. | `bash`, `curl`, `python3`; `nc` only for SSH reachability checks. | `RUN_HOST_SYNC=true INVENTORY_FILE=inventory-ci.yml NETWORK_ACCESS_POLICY_JSON='...' .github/scripts/network/preflight_network_access.sh` |
| `.github/scripts/network/wait_for_portainer_allowlist_propagation.sh` | Poll Portainer API until allowlist propagation permits current runner. | Env: `PORTAINER_API_URL`; optional `WAIT_TIMEOUT_SECONDS`, `POLL_INTERVAL_SECONDS`. | `0` on HTTP `200/401`; `1` on timeout or invalid input. | Idempotent polling behavior. | `bash`, `curl`. | `PORTAINER_API_URL=https://portainer-api.example.com .github/scripts/network/wait_for_portainer_allowlist_propagation.sh` |
| `.github/scripts/tfc/assert_tfc_workspace_local_mode.sh` | Enforce `operations=false` for local Terraform CLI apply workflow. | Env: `TFC_TOKEN`, `TFC_ORGANIZATION`, `TFC_WORKSPACE`. | `0` when workspace local mode is set; `1` otherwise. | Idempotent read-only assertion. | `bash`, `curl`, `jq`. | `TFC_TOKEN=... TFC_ORGANIZATION=my-org TFC_WORKSPACE=goodoldme-portainer .github/scripts/tfc/assert_tfc_workspace_local_mode.sh` |
| `.github/scripts/stacks/verify_trusted_stacks_sha.sh` | Verify requested stacks SHA ancestry plus observed GitHub CI trust signals against the stacks repo. | Arg: `<STACKS_SHA>`. Env: `STACKS_REPO_READ_TOKEN` or `GITHUB_TOKEN`; optional owner/repo/branch/api vars plus optional wait tuning vars. | `0` when ancestry passes, at least one CI signal exists, and every observed signal channel is green; `1` on invalid SHA, API failure, missing signals, pending checks, failed checks/statuses, or trust violations. | Idempotent read-only verification. | `bash`, `curl`, `jq`, GitHub API token with `contents:read`, `checks:read`, and `statuses:read` on the stacks repo. | `.github/scripts/stacks/verify_trusted_stacks_sha.sh 0123456789abcdef0123456789abcdef01234567` |
| `.github/scripts/tfc/wait_for_tfc_run.sh` | Poll Terraform Cloud run until terminal success/failure/timeout. | Args: `<RUN_ID> <WORKSPACE_NAME>`. Env: `TFC_TOKEN`; optional `TFC_API_URL`, `WAIT_TIMEOUT_SECONDS`, `POLL_INTERVAL_SECONDS`, `TFC_ORGANIZATION`. | `0` on `planned_and_finished` or `applied`; `1` on failed terminal states or timeout. | Idempotent polling behavior. | `bash`, `curl`, `jq`. | `TFC_TOKEN=... .github/scripts/tfc/wait_for_tfc_run.sh run-abc123 goodoldme-infra` |

## Workflow Stage Wrappers

These wrappers under `.github/scripts/stages/` are invoked by the Dagger pipeline phase modules in `ci/src/ci_pipeline/phases/` (or directly from GHA job steps in `orchestrator.yml`) and compose the lower-level helpers listed above.

For Infisical-backed wrappers, the calling phase module injects `INFISICAL_MACHINE_IDENTITY_ID`, `INFISICAL_PROJECT_ID`, and OIDC token vars into the container environment. The wrapper scripts call `setup_infisical` and `infisical run` themselves. Ansible stages run as host subprocesses (not inside Dagger containers), receiving env vars directly from the runner environment.

| Script | Invoked By | Responsibility |
|-------|------------|----------------|
| `.github/scripts/stages/ansible_run.sh` | `ci_pipeline/phases/ansible.py` (host subprocess on GHA runner) | Checks out the trusted `STACKS_SHA`, logs into Infisical, mints an ephemeral SSH certificate, and runs `ansible/playbooks/provision.yml` with optional tag scoping |
| `.github/scripts/stages/health_gated_redeploy.sh` | `ci_pipeline/phases/portainer.py` (inside Dagger container, `webhook` profile) | Checks out the trusted stacks ref, logs into Infisical, and triggers `trigger_webhooks_with_gates.sh` for the full Portainer-managed reconcile |
| `.github/scripts/stages/infra_apply.sh` | `orchestrator.yml` `infra-apply` GHA job (direct step) | Wraps `wait_for_tfc_run.sh` with the orchestrator’s manual-confirm and terminal-status expectations |
| `.github/scripts/stages/network_policy_sync.sh` | `ci_pipeline/phases/preflight.py` (inside Dagger container, `network` profile) | Builds the canonical access policy and syncs it to Terraform Cloud plus Infisical |
| `.github/scripts/stages/portainer_api_preflight.sh` | `ci_pipeline/phases/portainer.py` (inside Dagger container, `infisical` profile) | Reads `PORTAINER_API_URL` from Infisical, waits for allowlist propagation, and runs network reachability preflight checks |
| `.github/scripts/stages/portainer_apply.sh` | `ci_pipeline/phases/portainer.py` (inside Dagger container, `terraform` profile) | Verifies `operations=false`, then runs `terraform/portainer-root` init/plan/apply with optional `STACKS_SHA` pinning |
| `.github/scripts/stages/post_bootstrap_secret_check.sh` | `ci_pipeline/phases/portainer.py` (inside Dagger container, `infisical` profile) | Verifies that bootstrap-managed Portainer secrets already exist in Infisical before Terraform tries to use them |
| `.github/scripts/stages/secret_validation.sh` | `ci_pipeline/phases/preflight.py` (inside Dagger container, `infisical` profile) | Fail-closed validation for the exact secret sets required by the current plan toggles, rejecting seeded placeholder values as well as empty secrets |
| `.github/scripts/stages/tfc_speculative_preflight.sh` | `validate-terraform.yml` | Resolves the Terraform Cloud workspace name for speculative validation and fails early on missing inputs |

## Notes

- CI scripts are intended to run inside Dagger containers or as GHA job steps with explicit env contracts.
- Committed automation helpers pass `--projectId` explicitly for Infisical reads and writes; the repo root `infisical.json` is only a convenience for manual local debugging.
- Dagger pipeline phases (`ci/src/ci_pipeline/phases/`) pass required env vars (including `INFISICAL_MACHINE_IDENTITY_ID`, `INFISICAL_PROJECT_ID`, and OIDC tokens) into containers or the host subprocess environment before invoking these scripts.
- Composite query bootstrap action: `.github/actions/bootstrap-query-tools/action.yml` installs pinned `jq`/`yq` versions from `.github/ci/tool-versions.lock` with SHA256 verification for GitHub-hosted validation jobs. Cloud-runner jobs (used only by `validate-terraform.yml` `portainer-live-plan`) rely on the prebuilt `toolingDebian` runner image instead of runtime installation.

### Stacks Sub-Repo Tool Pinning

The `stacks/` sub-repo (`stacks-ci.yml` and `stacks-dispatch-redeploy.yml`) cannot reference the parent repository's `.github/actions/bootstrap-query-tools` composite action. As a result, tool versions (jq, yq) are pinned **inline** in those workflows with hardcoded versions and SHA256 checksums. These **must be kept in sync manually** with the canonical source of truth in `.github/ci/tool-versions.lock`.

| Tool | Parent canonical source | Stacks inline location |
|------|------------------------|----------------------|
| `yq` | `.github/ci/tool-versions.lock` (`YQ_VERSION`, `YQ_SHA256`) | `stacks/.github/workflows/stacks-ci.yml` (Install jq + yq step) |
| `jq` | `.github/ci/tool-versions.lock` (`JQ_VERSION`) | `stacks/.github/workflows/stacks-dispatch-redeploy.yml` (Install jq step) |

> **After any tool version bump** in `tool-versions.lock`, also update the corresponding inline version and SHA in the stacks workflows. The SHA values may differ between the parent and stacks repos if the downloaded artifact variants differ (e.g., different architectures or packaging formats).

- Trusted stacks SHA verification treats GitHub Checks and legacy commit statuses as separate signal channels. A SHA is trusted only when at least one channel exists and every channel that exists is green; check-runs-only and statuses-only repos are both valid.
- Archived scripts are retained for historical reference and should not be used in the normal deployment path.
- For operational deployment flows, see [Deployment Runbook](deployment-runbook.md).
